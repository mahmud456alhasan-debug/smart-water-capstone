# YOUR manual steps — Week 7 Session A report

Complete these, then tell Cursor: **"Week 7 manual steps done"** to finalize the PDF (screenshot + GitHub URL).

## 1. GitHub repository (~5 min)

```bash
cd "/home/mahmudul-robotics/Downloads/Software Development/ai_water_lab/capstone"
git init
git add .
git commit -m "Week 7 Session A: capstone scope and architecture"
git branch -M main
# Create empty repo on GitHub named smart-water-capstone, then:
git remote add origin https://github.com/YOUR_USERNAME/smart-water-capstone.git
git push -u origin main
```

## 2. Update prompt_log.md

Open `prompt_log.md` and replace:

`REPLACE_WITH_YOUR_REPO_URL`

with your real GitHub URL.

## 3. One screenshot (~1 min)

Take **one** screenshot of either:

- GitHub repo file list in browser, OR  
- Terminal: `tree -L 2` inside `capstone/`

Save as:

`/home/mahmudul-robotics/Downloads/Software Development/Lab-reports/week7_session_a_terminal.png`

## 4. Optional review (~5 min)

Skim `SCOPE.md` and `ARCHITECTURE.md`. Edit anything your instructor requires differently.

## 5. Upload to course platform

- PDF: `Lab-reports/Week7_SessionA_Report.pdf` (after agent rebuilds)
- GitHub link
- Or zip `capstone/` folder if LMS asks for files

**You do NOT need OpenCode screenshots.**
